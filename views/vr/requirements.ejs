<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Requirements</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/prism/1.25.0/themes/prism.min.css">
  <script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.25.0/prism.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.25.0/components/prism-javascript.min.js"></script>


  <link rel="stylesheet" href="/vr.css">
</head>
<body>
  <div class="container">
    <h1>Victory road - Requirements</h1>
    <article>
      <div class="tip">
        <h3>NOTE:</h3>
        <p>If you already have prior experience with coding in a language like javascript or python enough to where you know how to create your web server, just go ahead! This tutorial only serves as a way for people with less experience to create one.</p>
      </div> <br>
      <p>To create a web server in this tutorial, we're going to use node.js and the express library. There shouldn't be any other programs you need, because things like express and ngrok we will install as dependencies</p>
      <br>
      <h2>Requirement links:</h2>
      <p style="color:gray"> > <a href="https://nodejs.org/en" target="_blank" style="
        color: skyblue;
        text-decoration: underline;
        background-color: transparent;
        padding: 2px;
        margin: 0;
      ">Node.js</a></p>

      <br>
      <a href="/victory-road/node-environment">Continue</a>

      
    </article>
  </div>

<script>
  function copyText() {
      const parentElement = document.getElementById('toCopy');
      let textToCopy = '';

      for (let i = 0; i < parentElement.childNodes.length; i++) {
          const child = parentElement.childNodes[i];

          textToCopy += child.textContent;
      }

      navigator.clipboard.writeText(textToCopy)
          .then(function() {
              const button = document.getElementById('lua-copy-image')
              button.src = 'img/check.png'
              button.style.height = '25px'
          })
          .catch(function(error) {
              console.error('Unable to copy text to clipboard:', error);
          });
  }
</script>
</body>
</html>
